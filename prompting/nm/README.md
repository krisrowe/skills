# nm

Nevermind. Tells your agent to discard the last prompt as though it was never issued. No follow-up, no questions, no residual context.

Cross-platform — works with any AI agent that supports the SKILL.md format.

## Install

```bash
pipx install echomodel
em skills install nm
```

